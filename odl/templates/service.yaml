apiVersion: v1
kind: Service
metadata:
  name: odl-service
  namespace: openstack
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    #nodePort: 30001
    protocol: TCP
    name: neutron
  - port: 8101
    targetPort: 8101
    #nodePort: 30001
    protocol: TCP
    name: karaf
  - port: 8181
    targetPort: 8181
    #nodePort: 30002
    protocol: TCP
    name: northbound
  selector:
    app: odl
